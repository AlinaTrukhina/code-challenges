/*
abubanggheed — Today at 3:17 PM
Write a function that takes in at least 1 function as an input and returns a new function
that is the composition of the inputs.
e.g.
compose(
  x => x - 1,
  x => x * 2,
  x => x + ' s'
)(10)
'18 s'

compose(
  arg => arg.reduce((a, b) => a + b),
  x => x * x
)([-7, 1, 8])
4
*/

function compose([fun]) {
    // store input

}

// takes in functions, returns one callback function?





